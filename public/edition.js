/*
 * Browser extension entry point for the Career Canvas community edition.
 *
 * Premium editions can replace this file with an adapter that uses the
 * documented hooks without changing the core browser application.
 */

window.CareerCanvasEdition = Object.freeze({
	id: 'community'
});
