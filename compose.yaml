services:
  career-canvas:
    build: .
    container_name: career-canvas
    restart: unless-stopped
    ports:
      - "127.0.0.1:3080:3000"
    volumes:
      - career_canvas_data:/data

volumes:
  career_canvas_data:
